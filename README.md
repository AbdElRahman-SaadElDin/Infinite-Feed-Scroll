# Infinite-Feed-Scroll
Responsive infinite scroll feed built with React, TypeScript, React Query, and MSW. Includes mocked pagination, network latency simulation, loading indicators, retry on error, and smooth responsive UX.
